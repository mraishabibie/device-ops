# Enable AndroidX support for all library dependencies
android.useAndroidX=true
# Automatically convert legacy libraries to use AndroidX
android.enableJetifier=true

# Optimise Kotlin compilation speed
kotlin.incremental=true
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
